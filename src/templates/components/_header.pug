//- header.pug  header component

//- variables
- var navItems = ['Home', 'Projects', 'About']

header.header
    h1 Main Brand
    nav.nav
        ul.menu
            each item in navItems
                li: a(href='#')= item
